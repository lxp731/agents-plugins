{
  "id": "edge-tts-for-oc",
  "name": "Edge TTS for OpenClaw",
  "description": "Voice replies using Microsoft Edge TTS (cloud). ⚠️ Reply text is sent to Microsoft's servers for synthesis.",
  "version": "1.0.5",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "voice": {
        "type": "string",
        "description": "Edge TTS voice name. List voices: edge-tts --list-voices",
        "default": "zh-CN-YunxiaNeural"
      },
      "autoSpeak": {
        "type": "string",
        "enum": ["always", "auto"],
        "description": "always: speak every reply. auto: only speak when model calls the speak tool.",
        "default": "auto"
      }
    }
  },
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": ["speak"]
  }
}
